# 第十六轮更新 · 替换 1 个文件

> 这次只改了 `index.html`，`manifest.json` 和 `sw.js` 没变。
> 为省事，三个文件都放在包里，**只上传 index.html 覆盖即可**（另两个传不传都行）。

## 这次更新了什么

1. **成就值 → 积分**：全应用改名，包括侧边那个「成就」应用也改叫「积分」
2. **积分可以填负数**：想扣分就填 -5（快速记录、额外积分、专注设置、习惯都支持）
3. **习惯应用重做**
   - 新增**坏习惯**：设定每天/每周/每月允许几次，不超标得基础分，每超额一次扣分，每少做一次奖励
   - 好习惯改成**打卡计分**：每次得分 + 达标奖励 + 超一次奖励 + 少一次扣分
   - 周期奖励/扣分会**均摊到周期内每一天**，热力图每天都能看到贡献
   - **一个习惯一屏**：顶部 ‹ › 左右切换，不再所有习惯挤在一起
   - 热力图只看**每天积分**这一个元素，绿=得分 红=扣分
   - 可切**近14天 / 近4周 / 近8周**
4. **倒数日 → 事件提醒**：可精确到分钟；添加时**默认自动加「提前30分钟提醒」**（可取消）；
   每个事件可设**多个提醒**（提前 X 分钟 / 之后 X 分钟 / 具体时间）；支持**系统通知**
5. **便签优化**
   - 底部工具条改成**右侧竖排**（不占正文空间），面板从左边弹出
   - 插入图片后**立即显示**，不用退出重进
   - 右上角 ✕ 改成**「退出」**两个字
   - 长按「我的」不再弹出「复制」
6. **安装提示**：已经装成 App 后，不再显示「安装到桌面」卡片

---

# 以下为上一版说明（manifest/sw 相关）

## 之前为什么装不了

你仓库 `icons` 文件夹里的 5 个文件是微信传图后自动生成的哈希名，全是 `.jpg`：

```
5be548f3b3c694002c6e9b43fd923b7c.jpg
74ddc5d69c526bfe2107abd2d3ac6509.jpg
8d2db0982f048c75ee11f17706963996.jpg
cd7bfd655f451a0978ea351484c8feea.jpg
fbcf2b38e8f06a3690d9ba21962ed78c.jpg
```

而 manifest 里写的是 `icons/icon-192.png`……**一个都对不上**。
浏览器找不到图标 → 不满足 PWA 安装条件 → Edge 只能给"添加至手机"快捷方式。

## 这次怎么修的

**把图标直接内嵌进 manifest.json**（base64 data URI）。
不再引用任何外部图片文件，**你再也不用管 icons 文件夹**（里面那 5 张 jpg 留着或删掉都无所谓）。

同时修了：
- **天气自动重试**：open-meteo 在国内偶发失败，现在失败会自动重试 2 次（间隔 2 秒），不再动不动"天气暂不可用"
- **sw.js 缓存清单**：去掉了不存在的 `icons/*.png` 引用（之前会导致缓存安装失败）
- **新增「安装到桌面」按钮**：浏览器支持时，「我的」页顶部会出现一张卡片，点一下就能装

---

## 你要做的（3 步）

### 1. 下载 `LUMEN-修复包.zip`，解压

得到 3 个文件：`index.html`、`manifest.json`、`sw.js`

### 2. 上传到 GitHub 覆盖

进你的仓库 https://github.com/yhx-wt/lumen
→ **Add file → Upload files**
→ 把这 3 个文件拖进去
→ 底部 **Commit changes**

> ⚠️ 会提示"index.html 已存在"，没关系，同名的会自动覆盖。
> ⚠️ **不要**再拖 icons 文件夹。

### 3. 手机 Edge 里刷新 + 安装

1. 等 1~2 分钟让 GitHub Pages 重新部署
2. 手机 Edge 打开 `https://yhx-wt.github.io/lumen/`
3. **先删掉桌面上旧的快捷方式**（避免混淆）
4. 页面加载完 → 点底部 **⋮ 三点** 菜单
5. 这时应该能看到 **「安装此站点为应用」** 或 **「安装应用」**（不再是"添加至手机"）
6. 点它 → 确认安装

**如果菜单里还是只有"添加至手机"**：
打开「我的」页，最上方应该有一张 **「安装到桌面」** 卡片，点里面的 **立即安装** 按钮。

---

## 装完后

- 桌面出现独立图标，点开**全屏、无地址栏**
- 去「我的 → 设置 → 备份与恢复」**导入之前的 JSON 备份**
- 天气应该能正常显示了（如果在室外信号差，多等几秒会自动重试）

---

## 以后我更新了怎么办

我给你新的 `index.html`，你**只上传这一个文件**覆盖即可。

手机上的应用下次打开会自动拉新版本（Service Worker 是"网络优先"策略）。
如果打开还是旧版：把应用**完全关掉重开**一次，或长按图标卸载重装（数据已备份的话不丢）。

---

## 三个文件分别是什么

| 文件 | 作用 |
|---|---|
| `index.html` | 应用本体（786KB，含全部功能 + 内嵌 favicon 图标） |
| `manifest.json` | PWA 身份证：应用名、图标（内嵌）、全屏模式（132KB，大部分是图标数据） |
| `sw.js` | 离线缓存脚本，也是 PWA 能被安装的前提条件 |

**不需要 icons 文件夹了** —— 图标全部内嵌在这两个文件里。
